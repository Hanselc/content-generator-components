# Workspace base path. Request path fields (input_folder, output_folder,
# spec_path) are workspace-relative and resolved against this.
WORKSPACE_BASE_PATH=/home/hansel/documents/dev/projects/n8n

# Server port (default 8787)
PORT=8787